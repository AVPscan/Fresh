[![Build on Linux](https://github.com/AVPscan/Code_editor/actions/workflows/Build%20on%20Linux.yml/badge.svg)](https://github.com/AVPscan/Code_editor/actions/workflows/Build%20on%20Linux.yml)
[![Linux musl static +(Cross-Platform)](https://github.com/AVPscan/Code_editor/actions/workflows/release.yml/badge.svg)](https://github.com/AVPscan/Code_editor/actions/workflows/release.yml)

почти релиз *2~3
# 🌀 FreshVRAM (ex Balidan): Universal Terminal Monolith
> **"Планетарный" терминальный движок на базе монолитной видеопамяти и Dirty-Bit рендеринга.**

**Автор:** Алексей Васильевич Поздняков ([avp70ru@mail.ru](mailto:avp70ru@mail.ru))  
**Лицензия:** GPLv3  
**Дата создания:** Февраль 2026

---

## 🌍 Глобальная концепция (Законы Мира)

Архитектура проекта — это **Текстовая ОС (Terminal-Native)**, работающая напрямую с физической памятью через системные вызовы. Весь мир представлен как единый монолитный буфер (`VRam`), разделенный на функциональные слои.

1. **Пространство (`GlobalBuf`)**: Единая непрерывная память (~75 Мб для 10922x8192 Брайль (4096*2048 utf8) легко организовать теневые экраны при этом не забываем про масштаб шрифта, по сути мышью растянули и шрифт уменьшили, всё работает).
   - **Смещение**: Системный сектор. Настройка разрядности через `typedef uintptr_t Cell`.
   - **Мультиразрядность**: Все указатели и смещения адаптируются под 16/32/64/128/...-битные системы (Cell-based).

2. **Матрица (Planar VRAM)**:
   Разделение данных на независимые плоскости (слои) для максимального Cache-hit:
   - **G_DATA (Мясо)**: UTF-8 поток, 16 Кб на строку. Защита от цепочек UTF8 (CJK, Emoji, Zalgo (cp >= 0x1F300)).
   - **G_ATTRIBUTE (Энергия)**: Сверхбыстрая лента атрибутов (Цвет + Фон + вывести на экран) для токена целиком.
   - **G_TOKENS (Скелет)**: Атомарная структура мира. Для каждого токена хранится длина в байтах и визуальная ширина.
   - **G_LINE (Мозг)**: Компактные метаданные строк (общая длина, число токенов, визуальная длина).

3. **Световод (Engine)**: 
   Высокоскоростной рендерер, использующий **SWAR (SIMD Within A Register)**:
   - Сканирование атрибутов идет разом в разрядность процессора (для 64 по 8 токенов разом). 
   - Маска **`0x81818181...`** позволяет за один такт проверить (для 64 по 8 токенов разом) на наличие изменений.
   - При обнаружении изменения происходит вывод блока в терминал разом.

4. **Синхронизация и Графика**:
   - **Лупа (Viewport)**: Мгновенный прокрутка по миру простым смещением реального курсора.
   - **Брайль-Графика**: Использование символов Брайля (U+2800) превращает текстовую сетку в битмап-дисплей высокого разрешения.

---

## 🛠 Технологии и Оптимизация

- **Memory Management**: Прямые системные вызовы. Выравнивание по границам страниц (4 Кб).
- **Zero-Latency**: Неблокирующий ввод, отсутствие внешних библиотек и адаптивные задержки (чистый C11), игровой движок.
- **Адресная арифметика**: Вычисление всех смещений через битовые сдвиги (`<< 15` для строк, `<< 2` для токенов).
- **Unicode Resilience**: Однозначное декодирование UTF-8 и учет визуальной ширины (CJK/Emoji) на уровне "токенов-атомов".

---

## 🚀 Порядок реализации (Roadmap)

### Этап 1: Сотворение Среды (Си-фундамент)
- [x] Реализация аллокатора с выравниванием.
- [x] Разметка планарной памяти: `G_DATA`, `G_ATTR`, `G_TOKENS`, `G_LINE`.
- [x] Подготовка масок `0x81` для машинного слова `Cell` и не только.

### Этап 2: Кристальная Оптика (Рендерер)
- [x] **UTF-8 Decoder**: Однозначный парсинг потока.
- [x] **Visual Width Engine**: Авто определение визуальных размеров цепочек (CJK, Emoji, Combining Marks).
- [x] **Smart Append**: Логика формирования токенов и "прилипал" (эволюция символа в токене).

### Этап 3: Рождение Матрицы (VRAM Control)
- [x] **GOTO_COL (CHA)**: Прямое позиционирование реального курсора в окне терминала или в созданном окне внутри терминала.
- [x] **Dirty-Bit Logic**: Сверхбыстрый проход по атрибутам.
- [ ] **Viewport Engine**: Реализация "Лупы" (окно терминала или ...) для горизонтального и вертикального скроллинга.

### Этап 4: Жизнь в Монолите
- [ ] **Braille Graphics**: Библиотека отрисовки примитивов через точки Брайля.
- [ ] **World Scripting**: Интеграция логики управления контентом внутри `Vram`.
- [ ] **Deploy**: Запуск интерфейса, устойчивого к UTF8.


*Created in 2026. Designed for speed, logic, and clarity.*
