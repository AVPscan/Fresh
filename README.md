[![Build on Linux](https://github.com/AVPscan/Code_editor/actions/workflows/Build%20on%20Linux.yml/badge.svg)](https://github.com/AVPscan/Code_editor/actions/workflows/Build%20on%20Linux.yml)
[![Linux musl static +(Cross-Platform)](https://github.com/AVPscan/Code_editor/actions/workflows/release.yml/badge.svg)](https://github.com/AVPscan/Code_editor/actions/workflows/release.yml)

# 🌀 Balidan Linux: Code Editor / OS Concept
> **"Нормальный терминал" на базе Форт-логики и динамической видеопамяти.**

**Автор:** Алексей Васильевич Поздняков ([avp70ru@mail.ru](mailto:avp70ru@mail.ru))  
**Лицензия:** GPLv3  
**Дата создания:** Февраль 2026

---

## 🌍 Глобальная концепция (Законы Мира)

Архитектура проекта строится на жестком разделении логики управления ("Разум") и системы вывода ("Световод"), взаимодействующих через единое пространство памяти.

1. **Пространство (`GlobalBuf`)**: Единая непрерывная память. 
   - **Смещение 0**: Начало Системного Сектора. Размер фиксирован в ячейках (`cell`), а не в байтах.
   - **Мультиразрядность**: Все указатели и смещения имеют тип `cell` (размер регистра), что обеспечивает автоматическую адаптацию под 32/64/128-битные системы.

2. **Матрица (Динамическая VRAM)**:
   - Располагается сразу за системным сектором.
   - Каждая ячейка — кортеж: **[UTF-8 Data (4 байта)] [Атрибут (1 байт)]**.
   - **Никаких ESC-последовательностей внутри.** Только чистое «мясо» текста и графики.

3. **Световод (Си-движок)**: 
   - Бесшумный контроллер, работающий на уровне «железа». 
   - Видит изменение атрибута — шлет команду смены цвета. 
   - Видит данные — шлет их в терминал. 
   - Использует **Diff-отрисовку**: сравнивает текущее состояние с предыдущим кадром и выплевывает минимально необходимый поток байт.

4. **Синхронизация**:
   - `\n` (возврат каретки) в буфере — логический маркер конца строки для Форта.
   - Пробел — это пустота.
   - Всё остальное — символы Брайля (2x4 пикселя) или текст.

---

## 🛠 Технологии и Оптимизация

- **Static Analysis**: Чистый **C11**, без внешних зависимостей. Сборка через `make`.
- **Кроссплатформенность**: Linux / Windows / macOS.
- **Прецизионный тайминг**: Адаптивные задержки на базе `RDTSC` и замеры пропускной способности терминала (`GetC`).
- **Non-blocking UI**: Максимальная эффективность ввода-вывода без блокировок основного цикла.

---

## 🚀 Порядок реализации (Roadmap)

### Этап 1: Сотворение Среды (Си-фундамент)
- [x] Определение `cell` (typedef `intptr_t`).
- [x] Разметка Системного Сектора: `[CELL_SIZE]`, `[WORLD_LX/LY]`, `[VIEW_OX/OY]`, `[TERM_W/H]`, `[FLAGS]`, `[CPU_TICKS]`.
- [x] Инициализация Матрицы: Очистка пространства за системным сектором.

### Этап 2: Кристальная Оптика (Рендерер)
- [x] **UTF-8 Скан**: Собственная функция определения ширины символа без «черных ящиков».
- [x] **Diff-отрисовка**: Цикл минимизации потока байт в терминал.
- [x] **Фоновый Цвет**: Установка темы (Black/White) при старте.

### Этап 3: Рождение Разума (Kernal)
- [ ] **Deploy**: Копирование минимального набора Форт-примитивов в `GlobalBuf`.
- [ ] **Слова-Мосты**: 
    - `@SYS` — адрес системного сектора.
    - `AT` — установка координат.
    - `FETCH-XY / STORE-XY` — чтение и запись в экранную матрицу.
- [ ] **QUIT**: Запуск интерпретатора.

### Этап 4: Жизнь в Автомате
- [ ] **Физика**: Активация автоскроллинга и переноса строк на уровне Си-движка.
- [ ] **Проверка**: Запуск Форт-скрипта для одновременного вывода графики Брайля и текста.

---

*Created in 2026. Designed for speed, logic, and clarity.*
