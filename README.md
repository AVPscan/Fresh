[![Build on Linux](https://github.com/AVPscan/Code_editor/actions/workflows/Build%20on%20Linux.yml/badge.svg)](https://github.com/AVPscan/Code_editor/actions/workflows/Build%20on%20Linux.yml)
[![Linux musl static +(Cross-Platform)](https://github.com/AVPscan/Code_editor/actions/workflows/release.yml/badge.svg)](https://github.com/AVPscan/Code_editor/actions/workflows/release.yml)

# 🌀 FreshVRAM (ex Balidan): Universal Terminal Monolith
> **"Планетарный" терминальный движок на базе монолитной видеопамяти и Dirty-Bit рендеринга.**

**Автор:** Алексей Васильевич Поздняков ([avp70ru@mail.ru](mailto:avp70ru@mail.ru))  
**Лицензия:** GPLv3  
**Дата создания:** Февраль 2026

---

## 🌍 Глобальная концепция (Законы Мира)

Архитектура проекта — это **Текстовая ОС (Terminal-Native)**, работающая напрямую с физической памятью через системные вызовы `mmap`. Весь мир представлен как единый монолитный буфер (`GlobalBuf`), разделенный на функциональные слои.

1. **Пространство (`GlobalBuf`)**: Единая непрерывная память (~96 Мб для 2048x8192).
   - **Смещение 0**: Системный сектор. Настройка разрядности через `typedef uintptr_t Cell`.
   - **Мультиразрядность**: Все указатели и смещения адаптируются под 16/32/64/128-битные системы (Cell-based).

2. **Матрица (Planar VRAM)**:
   Разделение данных на независимые плоскости (слои) для максимального Cache-hit:
   - **G_DATA (Мясо)**: Сырой UTF-8 поток. 8 Кб на строку. Защита от "жирных" символов и Zalgo.
   - **G_ATTRIBUTE (Энергия)**: Сверхбыстрая лента атрибутов (Цвет + Dirty Bit `0x81`). 
   - **G_TOKENS (Скелет)**: Атомарная структура мира. Каждый токен знает свою `byte_len` и `vis_width`.
   - **G_LINE (Мозг)**: Компактные метаданные строк (6 байт на строку).

3. **Световод (Engine)**: 
   Высокоскоростной рендерер, использующий **SWAR (SIMD Within A Register)**:
   - Скан атрибутов идет машинными словами (`Cell`). 
   - Маска **`0x81818181...`** позволяет за один такт проверить X колонок на наличие изменений.
   - При обнаружении "грязи" происходит атомарный "бабах" (вывод блока) в терминал.

4. **Синхронизация и Графика**:
   - **Лупа (Viewport)**: Мгновенный скроллинг по миру 2048x1000 простым смещением указателя.
   - **Брайль-Графика**: Использование символов Брайля (U+2800) превращает текстовую сетку в битмап-дисплей высокого разрешения (4096x8192 точки).

---

## 🛠 Технологии и Оптимизация

- **Memory Management**: Прямые системные вызовы `mmap` / `munmap`. Выравнивание по границам страниц (4 Кб).
- **Zero-Latency**: Неблокирующий ввод (`O_NONBLOCK`) и отсутствие внешних библиотек (чистый C11).
- **Адресная арифметика**: Вычисление всех смещений через битовые сдвиги (`<< 13` для строк, `<< 2` для токенов).
- **Unicode Resilience**: Однозначное декодирование UTF-8 и учет визуальной ширины (CJK/Emoji) на уровне "токенов-атомов".

---

## 🚀 Порядок реализации (Roadmap)

### Этап 1: Сотворение Среды (Си-фундамент)
- [x] Реализация `mmap` аллокатора с выравниванием.
- [x] Разметка планарной памяти: `G_DATA`, `G_ATTR`, `G_TOKENS`, `G_LINE`.
- [x] Подготовка масок `0x81` для машинного слова `Cell`.

### Этап 2: Кристальная Оптика (Рендерер)
- [x] **UTF-8 Decoder**: Однозначный парсинг потока.
- [x] **Visual Width Engine**: Таблица ширин "на века" (CJK, Emoji, Combining Marks).
- [x] **Smart Append**: Логика формирования токенов и "прилипал" (эволюция символа в ячейке).

### Этап 3: Рождение Матрицы (VRAM Control)
- [x] **GOTO_COL (CHA)**: Прямое позиционирование курсора в физической сетке.
- [x] **Dirty-Bit Logic**: Сверхбыстрый проход по атрибутам.
- [ ] **Viewport Engine**: Реализация "Лупы" для горизонтального и вертикального скроллинга.

### Этап 4: Жизнь в Монолите
- [ ] **Braille Graphics**: Библиотека отрисовки примитивов через точки Брайля.
- [ ] **World Scripting**: Интеграция логики управления контентом внутри `GlobalBuf`.
- [ ] **Deploy**: Запуск "планетарного" интерфейса, устойчивого к UTF8.


*Created in 2026. Designed for speed, logic, and clarity.*
